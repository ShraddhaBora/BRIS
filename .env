# BRIS Frontend Configuration
# When the Python model server is running, set this to point to it.
# If the server is unreachable, the frontend automatically falls back
# to the formula-based computation.
VITE_MODEL_API_URL=http://localhost:8000
